// ===== Constants: tools and brush sizes =====
// Using constants/enums allows us to avoid "magic strings" and makes future extension easier.
export const TOOLS = Object.freeze({
  PENCIL: "pencil",
  ERASER: "eraser",
  BUCKET: "bucket",
});
